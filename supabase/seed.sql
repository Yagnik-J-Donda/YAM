-- Run after creating a test user. Replace the UUID below with auth.users.id.
do $$ declare uid uuid := '00000000-0000-0000-0000-000000000000'; hid uuid; office uuid; safe uuid; drawer1 uuid; entryway uuid; keydrawer uuid; bedroom uuid; closet uuid; shelf uuid; winterbox uuid; garage uuid; cabinet uuid; begin
insert into homes(owner_id,name,address) values(uid,'Canada Home','Toronto, Ontario') returning id into hid;
insert into locations(home_id,name,type) values(hid,'Office','room') returning id into office; insert into locations(home_id,parent_location_id,name,type) values(hid,office,'Black Safe','safe') returning id into safe; insert into locations(home_id,parent_location_id,name,type) values(hid,office,'Desk Drawer 1','drawer') returning id into drawer1;
insert into locations(home_id,name,type) values(hid,'Entryway','home_area') returning id into entryway; insert into locations(home_id,parent_location_id,name,type) values(hid,entryway,'Key Drawer','drawer') returning id into keydrawer;
insert into locations(home_id,name,type) values(hid,'Bedroom','room') returning id into bedroom; insert into locations(home_id,parent_location_id,name,type) values(hid,bedroom,'Closet','cupboard') returning id into closet; insert into locations(home_id,parent_location_id,name,type) values(hid,closet,'Top Shelf','shelf') returning id into shelf; insert into locations(home_id,parent_location_id,name,type) values(hid,shelf,'Blue Winter Box','box') returning id into winterbox;
insert into locations(home_id,name,type) values(hid,'Garage','room') returning id into garage; insert into locations(home_id,parent_location_id,name,type) values(hid,garage,'Tool Cabinet','cupboard') returning id into cabinet;
insert into items(home_id,current_location_id,name,category,description) values(hid,safe,'Canadian Passport','Documents','Keep with travel documents'),(hid,drawer1,'Spare iPhone Cable','Cables & Chargers','USB-C to Lightning'),(hid,winterbox,'Winter Jacket','Clothing','Navy parka'),(hid,keydrawer,'Car Keys','Other','Spare set'),(hid,cabinet,'Drill Machine','Tools','18V cordless drill'); end $$;
