import Link from "next/link"; import { AppShell } from "@/components/app-shell"; import { locations, items } from "@/lib/demo-data"; import { DoorOpen, ChevronRight, Plus, House } from "lucide-react";
export default function Locations(){ const roots=locations.filter(l=>!l.parentId); return <AppShell><div className="flex items-end justify-between"><div><p className="text-sm font-semibold text-[var(--accent-dark)]">Canada Home</p><h1 className="mt-2 font-display text-4xl font-semibold tracking-[-.04em]">Your locations</h1><p className="mt-2 text-sm text-[var(--muted)]">Explore every room, shelf, drawer and box.</p></div><button className="hidden rounded-full bg-[var(--ink)] px-4 py-3 text-sm font-semibold text-white sm:flex"><Plus size={18}/> Add location</button></div><div className="mt-8 rounded-[26px] bg-[#dfe9e4] p-5 sm:p-7"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-2xl bg-white"><House size={21}/></span><div><h2 className="font-semibold">Canada Home</h2><p className="text-xs text-[var(--muted)]">{locations.length} locations · {items.length} items</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{roots.map(l=><LocationRow key={l.id} id={l.id}/>)}</div></div></AppShell> }
function LocationRow({id}:{id:string}){ const l=locations.find(x=>x.id===id)!; const children=locations.filter(x=>x.parentId===id); const count=items.filter(i=>i.locationId===id).length; return <Link href={`/locations/${id}`} className="group flex items-center gap-4 rounded-[20px] bg-white p-4 shadow-sm hover:-translate-y-0.5"><span className="grid size-12 place-items-center rounded-2xl bg-[#f1f3f0] text-[var(--accent-dark)]"><DoorOpen size={22}/></span><span className="flex-1"><strong className="block text-sm">{l.name}</strong><small className="text-[var(--muted)]">{children.length} places · {count} items</small></span><ChevronRight size={18} className="text-[#aeb4b0]"/></Link>}
