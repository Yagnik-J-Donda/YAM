import { notFound } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { QrLabel } from "@/components/qr-label"; import { getItem,getLocation,fullPath } from "@/lib/demo-data";
export default async function QR({params}:{params:Promise<{kind:string,id:string}>}){const {kind,id}=await params;const record=kind==='item'?getItem(id):getLocation(id);if(!record)notFound();const path=kind==='item'?fullPath(getItem(id)!.locationId):fullPath(id);return <AppShell><QrLabel kind={kind as 'item'|'location'} id={id} name={record.name} path={path}/></AppShell>}
