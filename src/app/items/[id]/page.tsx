import Link from "next/link"; import { notFound } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { getItem, fullPath, locationPath } from "@/lib/demo-data"; import { ArrowLeft, MapPin, MoveRight, QrCode, TriangleAlert, CalendarDays, Hash, Banknote, History } from "lucide-react";
export default async function ItemPage({params}:{params:Promise<{id:string}>}){ const {id}=await params; const item=getItem(id); if(!item)notFound(); return <AppShell><Link href="/search" className="inline-flex items-center gap-2 text-sm font-semibold text-[var(--muted)]"><ArrowLeft size={17}/> Back</Link><div className="mt-6 grid gap-8 lg:grid-cols-[.75fr_1.25fr]"><div className="grid min-h-72 place-items-center rounded-[30px] bg-[#dfe9e4]"><span className="text-8xl">{item.icon==="passport"?"🛂":item.icon==="drill"?"🛠️":item.icon==="jacket"?"🧥":item.icon==="keys"?"🔑":"🔌"}</span></div><div><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-widest text-[var(--accent-dark)]">{item.category}</p><h1 className="mt-2 font-display text-4xl font-semibold tracking-[-.04em] sm:text-5xl">{item.name}</h1></div><span className="rounded-full bg-[#e4f0e9] px-3 py-1.5 text-xs font-semibold capitalize text-[var(--accent-dark)]">{item.status}</span></div><div className="mt-6 rounded-[22px] bg-[var(--ink)] p-5 text-white"><p className="flex items-center gap-2 text-xs font-semibold text-white/55"><MapPin size={15}/> CURRENT LOCATION</p><p className="mt-3 leading-7">{fullPath(item.locationId)}</p><p className="mt-3 text-xs text-white/45">Location is based on the last recorded update.</p></div><div className="mt-4 grid grid-cols-2 gap-3"><Link href={`/items/${id}/move`} className="flex min-h-13 items-center justify-center gap-2 rounded-2xl bg-[var(--accent)] px-4 font-semibold text-white"><MoveRight size={19}/> Move item</Link><Link href={`/qr/item/${id}`} className="flex min-h-13 items-center justify-center gap-2 rounded-2xl border border-black/10 bg-white px-4 font-semibold"><QrCode size={19}/> QR label</Link></div><div className="mt-8 grid gap-3 sm:grid-cols-2"><Fact icon={<Hash/>} label="Quantity" value={String(item.quantity)}/>{item.serial&&<Fact icon={<Hash/>} label="Serial number" value={item.serial}/>} {item.value&&<Fact icon={<Banknote/>} label="Approx. value" value={`$${item.value} CAD`}/>} {item.warranty&&<Fact icon={<CalendarDays/>} label="Warranty until" value="Sep 18, 2026"/>}</div><div className="mt-7"><h2 className="font-semibold">Notes</h2><p className="mt-2 text-sm leading-6 text-[var(--muted)]">{item.notes}</p></div><div className="mt-7 border-t border-black/[.07] pt-6"><h2 className="flex items-center gap-2 font-semibold"><History size={18}/> Movement history</h2><div className="mt-4 border-l-2 border-[#d7e4de] pl-5"><p className="text-sm font-semibold">Moved to {locationPath(item.locationId).at(-1)?.name}</p><p className="mt-1 text-xs text-[var(--muted)]">Aug 25, 2026 · by Yagnik</p></div></div><button className="mt-8 flex items-center gap-2 text-sm font-semibold text-red-600"><TriangleAlert size={17}/> Mark as missing</button></div></div></AppShell> }
function Fact({icon,label,value}:{icon:React.ReactNode,label:string,value:string}){return <div className="flex gap-3 rounded-2xl border border-black/[.06] bg-white p-4"><span className="text-[var(--accent)]">{icon}</span><span><small className="block text-[var(--muted)]">{label}</small><strong className="mt-1 block text-sm">{value}</strong></span></div>}
