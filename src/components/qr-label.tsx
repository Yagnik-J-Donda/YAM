"use client"; import { useEffect, useRef } from "react"; import QRCode from "qrcode"; import { Printer, Download } from "lucide-react";
export function QrLabel({kind,id,name,path}:{kind:"item"|"location";id:string;name:string;path:string}){const canvas=useRef<HTMLCanvasElement>(null);useEffect(()=>{if(canvas.current)QRCode.toCanvas(canvas.current,`${window.location.origin}/${kind==='item'?'items':'locations'}/${id}`,{width:260,margin:1,color:{dark:"#202522",light:"#ffffff"}})},[kind,id]);function download(){const a=document.createElement('a');a.download=`${name.toLowerCase().replaceAll(' ','-')}-qr.png`;a.href=canvas.current?.toDataURL()??'';a.click()}return <div className="mx-auto max-w-lg text-center"><div className="rounded-[32px] bg-white p-8 shadow-xl"><p className="text-xs font-semibold uppercase tracking-[.18em] text-[var(--accent-dark)]">Find My Home · {kind}</p><canvas ref={canvas} className="mx-auto mt-6 max-w-full"/><h1 className="mt-5 font-display text-3xl font-semibold">{name}</h1><p className="mx-auto mt-2 max-w-xs text-xs leading-5 text-[var(--muted)]">{path}</p><p className="mt-6 text-[10px] uppercase tracking-widest text-[#9aa09c]">Scan to view · Login required</p></div><div className="no-print mt-5 grid grid-cols-2 gap-3"><button onClick={()=>window.print()} className="flex h-13 items-center justify-center gap-2 rounded-2xl bg-[var(--ink)] font-semibold text-white"><Printer size={18}/> Print</button><button onClick={download} className="flex h-13 items-center justify-center gap-2 rounded-2xl border border-black/10 bg-white font-semibold"><Download size={18}/> PNG</button></div></div>}
