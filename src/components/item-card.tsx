import Link from "next/link"; import { Cable, KeyRound, Wrench, Shirt, BookOpen, MapPin, ArrowRight } from "lucide-react"; import type { Item } from "@/lib/demo-data"; import { fullPath } from "@/lib/demo-data";
const icons = { cable: Cable, keys: KeyRound, drill: Wrench, jacket: Shirt, passport: BookOpen };
export function ItemCard({ item, compact = false }: { item: Item; compact?: boolean }) { const Icon = icons[item.icon as keyof typeof icons] ?? BookOpen; return <Link href={`/items/${item.id}`} className="group flex items-center gap-4 rounded-[20px] border border-black/[.06] bg-white p-3.5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg"><div className="grid size-14 shrink-0 place-items-center rounded-2xl bg-[#eef3ef] text-[var(--accent-dark)]"><Icon size={25}/></div><div className="min-w-0 flex-1"><h3 className="truncate font-semibold">{item.name}</h3><p className="mt-1 flex items-start gap-1.5 text-xs leading-5 text-[var(--muted)]"><MapPin className="mt-0.5 shrink-0" size={13}/><span className={compact ? "line-clamp-1" : "line-clamp-2"}>{fullPath(item.locationId)}</span></p></div><ArrowRight size={17} className="shrink-0 text-[#b4b8b5]"/></Link>; }
