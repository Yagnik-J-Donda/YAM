"use client";
import Link from "next/link"; import { usePathname } from "next/navigation"; import { House, Search, ScanLine, MapPinned, UserRound, Plus } from "lucide-react"; import { clsx } from "clsx";
const nav = [{ href: "/", label: "Home", icon: House }, { href: "/search", label: "Search", icon: Search }, { href: "/scan", label: "Scan", icon: ScanLine, primary: true }, { href: "/locations", label: "Locations", icon: MapPinned }, { href: "/profile", label: "Profile", icon: UserRound }];
export function AppShell({ children }: { children: React.ReactNode }) { const path = usePathname(); return <div className="min-h-screen bg-[var(--canvas)] text-[var(--ink)]"><header className="sticky top-0 z-40 border-b border-black/[.06] bg-[rgba(248,247,243,.88)] backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-5"><Link href="/" className="flex items-center gap-3" aria-label="Find My Home"><span className="grid size-9 place-items-center rounded-xl bg-[var(--ink)] text-white"><House size={18}/></span><span className="font-display text-[19px] font-semibold tracking-[-.02em]">Find My Home</span></Link><div className="flex items-center gap-3"><div className="hidden text-right sm:block"><p className="text-xs text-[var(--muted)]">Current home</p><p className="text-sm font-semibold">Canada Home</p></div><button className="grid size-9 place-items-center rounded-full bg-[#dcebe6] text-sm font-semibold text-[var(--accent-dark)]" aria-label="Profile">YD</button></div></div></header><main className="mx-auto min-h-[calc(100vh-64px)] max-w-6xl px-5 pb-28 pt-7 md:pb-12 md:pt-10">{children}</main><nav className="fixed inset-x-0 bottom-0 z-50 border-t border-black/[.08] bg-white/95 px-3 pb-[max(10px,env(safe-area-inset-bottom))] pt-2 backdrop-blur-xl md:hidden"><div className="mx-auto grid max-w-md grid-cols-5">{nav.map(({ href, label, icon: Icon, primary }) => <Link key={href} href={href} className={clsx("relative flex min-h-14 flex-col items-center justify-end gap-1 text-[10px] font-semibold", path === href ? "text-[var(--accent-dark)]" : "text-[var(--muted)]")} >{primary ? <span className="absolute -top-7 grid size-14 place-items-center rounded-2xl bg-[var(--accent)] text-white shadow-lg"><Icon size={25}/></span> : <Icon size={21}/>}<span className={primary ? "mt-7" : ""}>{label}</span></Link>)}</div></nav><Link href="/items/new" className="fixed bottom-8 right-8 hidden items-center gap-2 rounded-full bg-[var(--ink)] px-5 py-3 text-sm font-semibold text-white shadow-xl md:flex"><Plus size={18}/> Add item</Link></div>; }
